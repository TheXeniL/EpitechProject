using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveLight : MoveButton {

    MoveButton moveLight = new MoveButton("BulbMenu");

    public MoveLight(string objectToPlace) : base(objectToPlace)
    {
    }

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
