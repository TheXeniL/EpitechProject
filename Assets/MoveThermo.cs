using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveThermo : MoveButton {

	MoveButton moveThermo = new MoveButton("ThermoMenu");

    public MoveThermo(string objectToPlace) : base(objectToPlace)
    {
    }

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
